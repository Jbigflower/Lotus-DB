	 主要实现两个文件，logging.py & setting.py。在实现过程中，需要注意：我们环境的pydantic使用的版本为 2.11 所以一定注意某些函数或约定可能发生了变动；另外，最关键的一点，在2.11 中，如果我们想要从 .env 中读取 一个字符串列表，那么一定要在类型中设置 Any，而非 List[str]，否则，即使加入了 field-vali……装饰器进行转换，也会报错，因为其强制先类型检查。
	